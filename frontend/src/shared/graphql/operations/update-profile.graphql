mutation UpdateProfile(
  $username: String
  $name: String
  $bio: String
  $avatarUrl: String
  $bannerUrl: String
) {
  updateProfile(
    username: $username
    name: $name
    bio: $bio
    avatarUrl: $avatarUrl
    bannerUrl: $bannerUrl
  ) {
    ...UserCore
  }
}
