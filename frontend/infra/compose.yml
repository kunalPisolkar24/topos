services:
  frontend:
    build:
      context: ../..
      dockerfile: frontend/Dockerfile
      args:
        VITE_ENV_TYPE: ${VITE_ENV_TYPE:-dev}
        VITE_GRAPHQL_URL: ${VITE_GRAPHQL_URL:-http://localhost:4000/graphql}
        VITE_BACKEND_URL: ${VITE_BACKEND_URL:-http://localhost:4000}
        VITE_CLOUDINARY_CLOUD_NAME: ${VITE_CLOUDINARY_CLOUD_NAME:-}
        VITE_CLOUDINARY_UPLOAD_PRESET: ${VITE_CLOUDINARY_UPLOAD_PRESET:-}
        VITE_ENABLE_MOCKS: ${VITE_ENABLE_MOCKS:-}
    container_name: ${FRONTEND_CONTAINER:-local-frontend}
    restart: unless-stopped
    ports:
      - "${FRONTEND_EXT_PORT:-3000}:${FRONTEND_INT_PORT:-3000}"
    environment:
      PORT: ${FRONTEND_INT_PORT:-3000}
      # Runtime config: single image serves dev/preview/prod via /config.js.
      # Same values as build args so `up` without rebuild still picks up env changes.
      VITE_ENV_TYPE: ${VITE_ENV_TYPE:-dev}
      VITE_GRAPHQL_URL: ${VITE_GRAPHQL_URL:-http://localhost:4000/graphql}
      VITE_BACKEND_URL: ${VITE_BACKEND_URL:-http://localhost:4000}
      VITE_CLOUDINARY_CLOUD_NAME: ${VITE_CLOUDINARY_CLOUD_NAME:-}
      VITE_CLOUDINARY_UPLOAD_PRESET: ${VITE_CLOUDINARY_UPLOAD_PRESET:-}
      VITE_ENABLE_MOCKS: ${VITE_ENABLE_MOCKS:-}
    networks:
      - app-network

networks:
  app-network:
    name: ${APP_NETWORK:-topos_local_network}
    driver: bridge
