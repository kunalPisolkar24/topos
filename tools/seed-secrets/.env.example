# Seed-secrets local overrides (optional)
# Used only when running the seeder via python-dotenv / Settings provider
AWS_ENDPOINT_URL=http://localhost:4566
AWS_REGION=ap-south-1
ENV_FILE=infrastructure/docker/prod/.env
