[project]
name = "ai-service"
version = "0.1.0"
description = "AI Intelligence Service"
authors = [
    {name = "Kunal Pisolkar",email = "55585892+kunalPisolkar24@users.noreply.github.com"}
]
requires-python = ">=3.12,<4.0.0"
dependencies = [
    "grpcio (>=1.83.0,<2.0.0)",
    "grpcio-health-checking (>=1.83.0,<2.0.0)",
    "pydantic-settings (>=2.14.2,<3.0.0)",
    "python-json-logger (>=4.1.0,<5.0.0)",
    "httpx (>=0.28.1,<0.29.0)",
    "pydantic (>=2.13.4,<3.0.0)",
    "tenacity (>=9.1.4,<10.0.0)",
    "bleach (>=6.4.0,<7.0.0)",
    "prometheus-client (>=0.26.0,<0.27.0)",
    "opentelemetry-sdk (>=1.30,<2.0)",
    "opentelemetry-exporter-otlp (>=1.30,<2.0)",
    "opentelemetry-instrumentation-grpc (>=0.51,<1.0)",
    "opentelemetry-instrumentation-httpx (>=0.51,<1.0)",
    "qdrant-client (>=1.15.0,<2.0.0)",
    "boto3 (>=1.34.0,<2.0.0)",
    "snowballstemmer (>=2.2.0,<3.0.0)",
    "langgraph (>=1.2.11,<2.0.0)",
    "langgraph-checkpoint-postgres (>=3.1.2,<4.0.0)",
    # The checkpoint saver pulls in bare psycopg, which needs libpq at
    # runtime; the binary extra ships its own and works in slim images.
    "psycopg[binary] (>=3.2.0,<4.0.0)",
    "langsmith (>=0.11.1,<0.12.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)",
    "pytest-asyncio (>=1.4.0,<2.0.0)",
    "pytest-cov (>=7.1.0,<8.0.0)",
    "ruff (>=0.16.1,<0.17.0)",
    "grpcio-tools (>=1.83.0,<2.0.0)",
    "testcontainers (>=4.11,<5.0)"
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "container: integration tests that run the service inside a docker container",
]

[tool.ruff]
exclude = ["src/generated"]

[tool.pyright]
exclude = ["src/generated/**/*.py"]

[tool.coverage.run]
omit = ["src/generated/*"]
